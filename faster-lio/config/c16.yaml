common:
    lid_topic: "/rslidar_points"
    imu_topic: "/imu/data"
    time_sync_en: false         # ONLY turn on when external time synchronization is really not possible
    
preprocess:
    lidar_type: 4                # 1 for Livox serials LiDAR, 2 for Velodyne LiDAR, 3 for ouster LiDAR, 4 for C16 Leishen Lidar
    scan_line: 16
    blind: 1.0
    z_min_range: -0.5     # [추가] 야외 환경 최적화 Z축 하한값 (바닥 노이즈 제거)
    z_max_range: 1.5      # [추가] 야외 환경 최적화 Z축 상한값 (하늘 허상 제거)
    time_scale: 1e-3

mapping:
    acc_cov: 0.1
    gyr_cov: 0.1
    b_acc_cov: 0.0001
    b_gyr_cov: 0.0001
    fov_degree:    180
    det_range:     150.0
    extrinsic_est_en:  true      # true: enable the online estimation of IMU-LiDAR extrinsic
    extrinsic_T: [ -0.011, -0.02329, 0.04412 ]
    extrinsic_R: [1, 0, 0,
                  0, 1, 0,
                  0, 0, 1]

publish:
    tf_imu_frame: "base_link"
    tf_world_frame: "map"
    path_publish_en:  false
    scan_publish_en:  true       # false: close all the point cloud output
    scan_effect_pub_en: true    # true: publish the pointscloud of effect point
    dense_publish_en: false       # false: low down the points number in a global-frame point clouds scan.
    scan_bodyframe_pub_en: true  # true: output the point cloud scans in IMU-body-frame

path_save_en: true

pcd_save:
    pcd_save_en: true
    interval: -1                 # how many LiDAR frames saved in each pcd file; 
                                 # -1 : all frames will be saved in ONE pcd file, may lead to memory crash when having too much frames.
feature_extract_enable: false
point_filter_num: 3
max_iteration: 7
filter_size_surf: 0.3
filter_size_map: 0.3             # 暂时未用到，代码中为0， 即倾向于将降采样后的scan中的所有点加入map
cube_side_length: 1000

ivox_grid_resolution: 1.5        # /////[수정]///// 기본 0.2 → 1.5로 증가함. 방대한 야외 조건에 의해서.
ivox_nearby_type: 18             # 6, 18, 26
esti_plane_threshold: 0.05        # default=0.1
